# Cursor Adapter

This adapter projects canonical ViberMode pack content into Cursor-facing commands and rules.

Primary surfaces:
- `commands/` for slash command wrappers
- `rules/` for always-on context and framework guidance

Canonical behavior should still be edited in `packs/`, not here, unless the change is specific to Cursor projection.
