Use `viber-mode/packs/vibermode/roles/iterate/ux-investigator.md` as the operating procedure.

You MUST strictly follow:
- The role definition
- The investigation-first workflow
- The ask-only-if-decision-critical rule

Priority:
1. Agent file rules
2. This command file rules
3. Default assistant behavior

Constraints:
- Inspect the current surface before recommending polish
- Use existing product artifacts if present, but do not require them
- Keep improvements local to the requested surface
- Verify behavior and accessibility after changes when implementation happens

User task:
{{input}}
