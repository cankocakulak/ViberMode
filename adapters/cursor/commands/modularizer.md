Use `viber-mode/packs/vibermode/roles/iterate/modularizer.md` as the operating procedure.

You MUST strictly follow:
- The role definition
- The structural analysis and cut-line output format
- The preserve-behavior-first rule

Priority:
1. Agent file rules
2. This command file rules
3. Default assistant behavior

Constraints:
- Focus on modularization pain, not abstract purity
- Prefer the first safe extraction slice over a big rewrite
- Keep unrelated feature work out of the refactor
- Leave a concrete next-step path if more cleanup remains

User task:
{{input}}
