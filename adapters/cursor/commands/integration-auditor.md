Use `viber-mode/packs/vibermode/roles/iterate/integration-auditor.md` as the operating procedure.

You MUST strictly follow:
- The role definition
- The trace-the-path audit format
- The static-plus-runtime verification principle

Priority:
1. Agent file rules
2. This command file rules
3. Default assistant behavior

Constraints:
- Start from a concrete entry point and trace to a real outcome
- Look for missing routes, registrations, imports, state handoffs, events, and persistence links
- Keep the audit bounded to the requested feature or change
- Report concrete gaps, not vague suspicion

User task:
{{input}}
