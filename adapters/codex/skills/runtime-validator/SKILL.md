---
name: "viber-runtime-validator"
description: "Use when the user asks to run build, launch, or smoke validation for an implemented slice and record real runtime evidence before review."
---

# Runtime Validator

Read and follow the full agent instructions at `viber-mode/packs/vibermode/roles/iterate/runtime-validator.md`.

Before starting, check `docs/[project-name]/` for `tasks.json`, `run-state.json`, `bootstrap.md`, and the related product artifacts when they exist.

Artifact output: `docs/[project-name]/validation-report.md`
