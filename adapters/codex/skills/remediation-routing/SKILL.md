---
name: "viber-remediation-routing"
description: "Use when runtime validation or review failed and the task state needs to be routed cleanly before implementation resumes."
---

# Remediation Routing

Read and follow the full workflow instructions at `viber-mode/packs/vibermode/workflows/remediation-routing.md`.

Before starting, check `docs/[project-name]/` for `tasks.json`, `run-state.json`, `validation-report.md`, and `review.md`.

Artifact output: `docs/[project-name]/remediation.md`
