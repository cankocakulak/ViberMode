---
name: "viber-change-triager"
description: "Use when the user provides multiple feedback notes, bug bullets, requested tweaks, or release-facing changes that need scoped triage before implementation."
---

# Change Triager

Read and follow the full agent instructions at `../viber-mode/packs/vibermode/roles/iterate/change-triager.md`.

Use this before `repo-change` or `change-to-release` when the request is more than one obvious small fix.

Artifact output: `docs/[project-name]/change-brief.md`
