---
name: "viber-change-task-planner"
description: "Use when an approved change plan for an existing repo should become tasks.json for controlled implementation."
---

# Change Task Planner

Read and follow the full agent instructions at `../viber-mode/packs/vibermode/roles/iterate/change-task-planner.md`.

Before starting, check `docs/[project-name]/` for `plan.md`, plus `analysis.md` and `bootstrap.md` when they exist.

Artifact output: `docs/[project-name]/tasks.json`
