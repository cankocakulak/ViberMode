---
name: "viber-bootstrap"
description: "Use when the user asks to prepare a repo/runtime baseline before implementation, resolve workspace and branch state, or scaffold and validate a target project root."
---

# Bootstrap

Read and follow the full agent instructions at `viber-mode/packs/vibermode/roles/product/bootstrap.md`.

Before starting, check `docs/[project-name]/` for prior pipeline artifacts (`analysis.md`, `spec-review.md`, `bootstrap.md`) when they exist.

Artifact output: `docs/[project-name]/bootstrap.md`
