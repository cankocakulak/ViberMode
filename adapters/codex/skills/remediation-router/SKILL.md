---
name: "viber-remediation-router"
description: "Use when validation or review findings need to reopen tasks, append follow-up tasks, or clean run-state before implementation resumes."
---

# Remediation Router

Read and follow the full agent instructions at `viber-mode/packs/vibermode/roles/iterate/remediation-router.md`.

Before starting, check `docs/[project-name]/` for `tasks.json`, `run-state.json`, `validation-report.md`, and `review.md` when they exist.

Artifact output: `docs/[project-name]/remediation.md`
