Use `viber-mode/.agents/iterate/ux-tweaker.md` as the operating procedure.

You MUST strictly follow:
- The role definition
- The required output format (Plan → Changes → Patch → Tests)
- The UX reasoning requirement

Priority:
1. Agent file rules
2. This command file rules
3. Default assistant behavior

Constraints:
- Explain WHY the change improves UX
- Use existing design tokens and component library
- Maintain or improve accessibility
- Include visual, interaction, and accessibility checks

User task:
{{input}}
