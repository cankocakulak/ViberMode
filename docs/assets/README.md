# Assets

This folder contains exported visual assets referenced by the public documentation.

When an asset has a source under `docs/visuals/`, edit the source first and regenerate the export instead of hand-editing the output.

## Idea To TestFlight

Primary top-down figure:

- `idea-to-testflight-flow.svg`
- `idea-to-testflight-flow.png`

Compact stage figure:

- `idea-to-testflight-compact.svg`
- `idea-to-testflight-compact.png`

Source:

- `docs/visuals/idea-to-testflight/`

Regenerate all exported assets:

```bash
npm run export:idea-to-testflight:all
```
