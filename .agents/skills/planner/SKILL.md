---
name: "viber-planner"
description: "Use when the user asks to investigate a bug, plan a feature, figure out how to add something, trace a problem, analyze root cause, or think through an approach before implementing."
---

# Planner

Read and follow the full agent instructions at `viber-mode/.agents/iterate/planner.md`.

Output: Analysis → Strategy (root cause/approach, changes required, implementation hints, verification).
